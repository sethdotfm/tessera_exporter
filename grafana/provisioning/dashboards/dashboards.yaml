# tessera_exporter grafana/provisioning/dashboards/dashboards.yaml // Version 1.1.0
# https://github.com/sethdotfm/tessera_exporter
apiVersion: 1

providers:
  - name: tessera_exporter
    type: file
    updateIntervalSeconds: 5
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
